import React from 'react';
import './homepage.css'; // Import the CSS file

const HomePage = () => {
    return (
        <div className="homepage">
            <h1>Welcome to Claim Approval Process</h1>
        </div>
    );
};

export default HomePage;